name: "cli.w3m"
description: "Text-based web browser."
type: "cli"
command: "w3m"
inputs:
  args: "List of w3m flags and URL(s)."
  stdin: "Optional stdin string."
  cwd: "Optional working directory."
outputs: "Rendered text output on stdout."
nix_package: "w3m"

